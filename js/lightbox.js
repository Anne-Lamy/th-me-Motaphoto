document.addEventListener('DOMContentLoaded', function () {
    
    // AFFICHAGE DE LA LIGHTBOX :

    /* Récupérer la lightbox.
    const lightbox = document.getElementById('lightbox');
    // Récupérer le lien screen.png.
    const screenLinks = document.querySelectorAll('.screen-link');
    // Récupérer l'élément <span> qui ferme la lightbox.
    const spanClose = document.querySelector(".box-close");

    // Lorsque l'utilisateur clique sur un lien screen.png, la lightbox s'affiche.
    screenLinks.forEach(link => {
        link.onclick = function(event) {
            event.preventDefault();
            lightbox.style.display = "block";
        }
    });
    // Lorsque l'utilisateur clique sur <span> (x), la lightbox disparait.
    spanClose.onclick = function(event) {
        event.preventDefault();
        lightbox.style.display = "none";
    }
    // Lorsque l'utilisateur clique n'importe où en dehors de la lightbox, elle se ferme.
    window.onclick = function(event) {
        if (event.target == lightbox) {
            lightbox.style.display = "none";
        }
    }*/

});